package com.example.autoclicker

import org.opencv.core.Mat

class Assets {
    fun getStairAssets(): List<Mat> {
        return emptyList() // 실제 템플릿 로드
    }
}

class MultiTemplateMatch {
    fun predict(templates: List<Mat>, img: Mat): List<Any> {
        return emptyList() // OpenCV 템플릿 매칭
    }
}
